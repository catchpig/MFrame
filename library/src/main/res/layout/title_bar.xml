<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/title_bar"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:visibility="gone"
    android:layout_height="50dp"
    android:background="@color/c_1e81d2">
    <TextView
        android:id="@+id/title"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:textSize="18sp"
        android:gravity="center"
        android:maxLines="1"
        android:textColor="@color/c_fff"
        tools:text="标题"/>
    <ImageView
        android:id="@+id/back"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_centerVertical="true"
        android:layout_marginLeft="10dp"
        tools:src="@mipmap/close"
        android:layout_gravity="center_vertical"/>
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:paddingRight="10dp"
        android:layout_gravity="right"
        android:orientation="horizontal">
        <ImageView
            android:id="@+id/rightFirstDrawable"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:visibility="gone"
            tools:src="@mipmap/close"
            android:layout_gravity="center_vertical"/>
        <TextView
            android:id="@+id/rightFirstText"
            android:layout_width="40dp"
            android:layout_height="match_parent"
            android:visibility="gone"
            android:textColor="@color/c_fff"
            android:gravity="center"
            android:textSize="15sp"
            tools:text="第1"/>
        <ImageView
            android:id="@+id/rightSecondDrawable"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:visibility="gone"
            tools:src="@mipmap/close"
            android:layout_gravity="center_vertical"/>
        <TextView
            android:id="@+id/rightSecondText"
            android:layout_width="40dp"
            android:visibility="gone"
            android:layout_height="match_parent"
            android:textColor="@color/c_fff"
            android:gravity="center"
            android:textSize="15sp"
            tools:text="第2"/>
    </LinearLayout>

</FrameLayout>